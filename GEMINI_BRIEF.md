# KnightSlide (MD) — 제미나이 리뷰용 현황 요약

소스 기준일: 2026-08-24. 수치·규칙은 `src/config.js`, `src/game.js`, `src/meta.js`, `src/skills-ui.js`, `src/admin.js`, `package.json`의 **현재 기본값**이다. 어드민으로 런타임에 바뀐 값은 반영되지 않는다. 레거시 `SPEC.md`는 사거리·발사속도 등이 코드와 어긋나므로 이 파일을 우선한다.

---

## 1. 한 줄 소개 + 기술 스택

**KnightSlide**는 하단에서 병력을 직선 발사하고, 같은 티어를 머지해 전열을 키우며, 상단 웨이브라인에 붙은 적 무리와 줄다리기하는 중세 머지 디펜스 프로토타입이다.

| 항목 | 값 |
|------|-----|
| 패키지 | `md-knightslide` 0.1.0 (private, `"type": "module"`) |
| 스택 | Vite `^8.2.1`, Vanilla JS, HTML5 Canvas 2D, Matter.js `^0.20.0` |
| 캔버스 | **450 × 800** (`index.html` title: `MD — KnightSlide`) |
| 물리 | 아군만 Matter 바디(중력 0). 적·라인·궁수·벽은 비물리 |
| 저장 | 메타만 `localStorage` (`md.knightslide.meta.v1`). 런 점수·웨이브는 저장 안 함 |
| 좌표계 | 좌상단 원점, Y 증가 = 아래. 라인 시작 **90**, 마지노선 **660**, 발사대 **700** |

전장 슬롯은 스킬 `boardWidth`로 **3 → 5 → 7**칸. extraInset = `(7 − cols) / 2 × 50` → 플레이어블 가로 **250 / 350 / 450**.

---

## 2. 코어 루프

1. **직선 발사**: 포인터 X로 조준, 놓으면 수직 위 등속 돌진(`launchSpeed` 720 px/s). 각도/슬링샷 당김 없음. 짧은 버스트(0.12–0.28초) 후 `settled` → 진군 테이블로 행군.
2. **머지**: 같은 티어 아군이 충돌하면 +1티어. 만피 조건 없음. T9+T9 → 랜덤 영웅(아서/잔느/발키리). 합성체는 즉시 정착해 전진.
3. **웨이브라인 줄다리기**: 라인에 **접촉한 전열 적만** 전진에 기여. `netSpeed = baseLineSpeed + Σ(적.speed × liveMult) − Σ(교전 아군 저지력)`. +아래 / −위.
4. **패배**: 라인이 Y **660**에 닿으면 게임오버. 유닛 전멸은 패배가 아님. 방어벽이 있으면 접촉 시 HP를 잃고 라인을 밀어냄(그 충격으로 HP 0이 되어도 밀치기는 적용, **다음** 접촉이 게임오버).
5. **빈 라인**: 라인 접촉 적이 없으면 **아래로 전진하지 않음**. 합류 행군 중인 적만 있어도 동일. 전열 정착 아군 저지력으로 시작 위치(Y 90)까지 **밀어올림**.
6. **뒷열**: 스폰은 상단(Y 90)에서 슬롯까지 내려와 착지. 뒷열은 라인 접촉(`|y − lineY| ≤ 8`) 후에만 푸시. 앞열 사망 시 열마다 0..n으로 compact.

보스: 처치 수 `10 + wave×3` 충족 → 경고 1.6초 → 오크 워로드. 보스 처치 시 웨이브 +1, 궁수 탄약 재충전, 추가 XP `200 × 클리어한 웨이브`.

---

## 3. 주요 개편 연혁 (리뷰에 필요한 것만)

| 개편 | 현재 의미 |
|------|-----------|
| 슬링샷 → 직선 | README는 아직 「슬링샷처럼」이라고 하지만, 구현은 하단 X 조준 + 수직 발사. |
| 개별 적 → 웨이브라인 | 적은 물리 바디가 아니라 라인 슬롯에 부착. 라인 전체가 아래로 온다. |
| 영웅 15초 수명 → 사명 승천 | 타이머 없음. 이 영웅이 적에게 가한 피해 **80,000**을 채우면 승천. HP로 죽으면 승천 없음. |
| 진군 스킬이 발사 관성에 가려짐 → 버스트 후 행군 | 돌진이 라인까지 실어다 주면 진격 스킬이 안 보임. 지금은 0.12–0.28초 버스트 후 `advanceSpeedByRank`로 걸음. |
| 전장 3 / 5 / 7 | `boardWidth` 랭크 0/1/2. 측면 벽·슬롯 X가 런타임에 재배치. |
| 사거리 축소 | 근접 열 전투감. T1 36 → T10 168 (구 SPEC의 40–225보다 짧음). 적 `enemyReach` **24** (구 SPEC 50). |
| 뒷열 접촉 후에만 푸시 + compact | 구멍에 떠 있는 적·합류 중 적은 라인 속도에 안 넣음. 처치 시 같은 열을 앞으로 밀집. |

---

## 4. 현재 수치 핵심

### 아군 T1–T10 (`UNITS`)

저지력 `stop` = 교전 중 라인 감속(및 빈 라인 전열 푸시) px/초. 사거리 `range`는 공격 도달만(전진은 전원 라인 앞까지).

| T | 이름 | r | HP | ATK | range | stop | 특수 |
|---|------|---|----|-----|-------|------|------|
| 1 | 민병대 | 16 | 50 | 5 | **36** | 4 | — |
| 2 | 신병 | 18 | 110 | 12 | **42** | 6 | — |
| 3 | 경보병 | 20 | 240 | 25 | **50** | 9 | — |
| 4 | 중보병 | 22 | 500 | 55 | **58** | 13 | — |
| 5 | 기사단원 | 24 | 1050 | 120 | **70** | 18 | 클레브 반경 56, 0.5배 |
| 6 | 근위대장 | 26 | 2200 | 250 | **82** | 24 | 기절 10% / 0.5초 |
| 7 | 성기사 | 28 | 4600 | 520 | **96** | 32 | 2초마다 반경 110, 최대HP 4% 회복 |
| 8 | 드래곤가디언 | 30 | 9500 | 1100 | **112** | 42 | 화상 DPS = ATK×0.20, 3초 |
| 9 | 대원수 | 33 | 19000 | 2300 | **128** | 55 | 4초마다 사거리 내 ATK×0.3 |
| 10 | 영웅 | 38 | 50000 | 6500 | **168** | 90 | 사명 + 히어로 스킬 |

발사 기본 티어: T1 **75%**, T2 **25%**, T3 0%(스킬 `higherTier`로만). 공격 틱 **0.8초**.

### 적 (`MONSTERS`) — 기본 HP/ATK. 스폰 시 `waveMultiplier × liveMult`

`speed` = 라인 가속 기여 px/초. **웨이브 배율은 speed에 안 곱함**, `liveMult`만 곱함. 스켈레톤 speed 0 → 라인을 안 민다.

| key | 이름 | r | HP | ATK | speed | 점수 | 기타 |
|-----|------|---|----|-----|-------|------|------|
| goblin | 고블린 | 15 | 40 | 4 | 3 | 10 | 풀 가중 55 |
| orc | 오크 | 20 | 180 | 18 | 1.5 | 25 | 가중 25 |
| skeleton | 스켈레톤 | 18 | 120 | 14 | **0** | 20 | 웨이브 2+, 가중 20 |
| troll | 동굴 트롤 | 28 | 1200 | 80 | 1 | 100 | 웨이브 3+, 가중 8, 재생 12/초 |
| boss | 오크 워로드 | 45 | 2300 | 85 | **38** | 500 | 라인 중앙 착지, 겹치면 밀어냄 |

적 근접 사거리 = `적.r + enemyReach(24)`.

스폰 간격: `max(0.7, 2.3 − wave×0.15) × (보스 중이면 1.8)` 후 ×(0.7~1.3 랜덤).

### 웨이브 곡선 (`waveMultiplier`)

- 웨이브 1–10: `1 + (wave−1)×0.10` → W1 = **1.00**, W10 ≈ **1.90**
- 웨이브 11+: 위 값 × `1.3^(wave−10)` → W11 ≈ **2.47**, 이후 지수
- 실효 배율 = 곡선 × 실시간 `liveMult`(기본 1.0, 0.5–5.0, 0.1 단위). HP는 스폰 스냅샷, ATK·라인 가속은 liveMult 즉시 반영
- 처치 필요: `10 + wave×3` (W1 = 13)

### 진군 R0–R5 (px/s)

`BALANCE.advanceSpeedByRank`. 스킬 `advance` 랭크가 인덱스. `perRank: 0.80`은 **미사용**(테이블이 이동을 담당).

| 랭크 | 0 (스킬 없음) | 1 | 2 | 3 | 4 | 5 |
|------|---------------|---|---|---|---|---|
| px/s | **28** | **34** | **42** | **52** | **64** | **80** |

발사 쿨: 기본 **1.15초**, 스킬 `launchCd` 랭크당 −0.16초, 하한 **0.25초**.  
적 합류 속도: **80 px/s** (라인이 내려가면 `max(80, 라인하강 + 20)`으로 따라잡음).

### 영웅 사명 / 승천

| 항목 | 값 |
|------|-----|
| 사명 | 이 영웅 바디가 적에게 가한 피해 **80,000** (`HERO_MISSION_KILLS` 25는 코드에 있으나 킬 모드 스위치 없음 → 미사용) |
| 아서 | 3초마다 전체 적에게 ATK×0.4 |
| 잔느 | 오라 140px, 비영웅 기본공격 ×1.5 (영웅은 버프 대상 아님) |
| 발키리 | 0.3초마다 사거리 내 ATK×0.12 |
| 승천 | 전체 적에게 `ATK×1.5` 폭발, 라인 Y=90 리셋, 슬로모 0.3초, T**5** 잔류, 점수 +400 |
| HP 사망 | 승천 없음 |

---

## 5. 스킬 트리 (해금 레벨 + 전장)

레벨업마다 포인트 1. 랭크 N 해금 레벨 = `unlockLevel + (N−1)×rankLevelStep`(기본 step 1). 구매는 즉시 현재 런에 반영.

| 트리 | 스킬 | 최대 | 해금 | 랭크별 레벨 | 효과 요약 |
|------|------|------|------|-------------|-----------|
| 전투 | 발사 쿨감 | 5 | 1 | 1,3,5,7,9 (`step` 2) | −0.16초/랭크, 최저 0.25 |
| 전투 | 진격 속도 | 5 | 1 | 1–5 | 표 28→80 px/s |
| 전투 | 재생 | 5 | 1 | 1–5 | 전투·전열만, 최대HP 0.4%/초/랭크 |
| 전열 | 저지력 증가 | 5 | 1 | 1–5 | +8%/랭크 |
| 전열 | **전장 확장** | **2** | **5** | **R1 Lv5 → 5칸, R2 Lv7 → 7칸** | 기본 **3칸** |
| 머지 | 상위 병사 확률 | 5 | 3 | 3–7 | T2 +3%/랭크. T3는 랭크 3부터 +2%/랭크 |
| 머지 | 머지 충격 | 5 | 3 | 3–7 | 피해 18·반경 30+6·넉백 6·회복 4%/랭크 |
| 방어벽 | 방어벽 설치 | 1 | 3 | Lv3 | 기본 HP 3, 밀치기 70px |
| 방어벽 | 방어벽 강화 | 5 | 5 | 5–9, 선행 벽 | +2 HP/랭크 |
| 방어벽 | 밀치기 거리 | 5 | 5 | 5,7,9,11,13 (`step` 2) | +22px/랭크 |
| 궁수 | 궁수 해금 | 1 | 8 | Lv8, 선행 벽 | 벽 위 궁수 1 |
| 궁수 | 궁수 수 | 4* | 8 | 선행 궁수 | 해금 1 + 랭크. 실제 상한 `archerMax` **3**이라 유효 최대 랭크 2 |
| 궁수 | 궁수 사거리 | 5 | 8 | 8–12 | 기본 160 +36/랭크 |
| 궁수 | 궁수 공격력 | 5 | 8 | 8,10,12,14,16 (`step` 2) | 기본 7 +4/랭크 |
| 궁수 | 궁수 탄약 | 5 | 8 | 8–12 | 기본 10 +4/웨이브 |

XP 곡선 1→2 … 9→10: 800, 1300, 2000, 2800, 3800, 5200, 7000, 9200, 12000. Lv10+는 12000 + 3000×(level−9). 목표 주석: Lv2 ≈ 1웨이브 보스 직후, Lv5 ≈ 웨이브 5–7, Lv10은 장기.

---

## 6. 어드민으로 바꿀 수 있는 것

**A 키** 또는 ⚙. 값은 즉시 `config` 객체에 쓰임(새로고침하면 코드 기본값으로 복귀). 메타 랭크 자체는 어드민이 아니라 스킬 패널.

- **실시간 난이도** `liveMult` 0.5–5.0 (HUD ± 및 +/− 키도 동일). 살아있는 적 HP 비율 유지 리스케일, ATK·라인 가속 즉시
- **전역**: 라인 속도, 스폰 간격, 발사 쿨/속도, 진군 R0–R5, 적 합류 속도, 보스 집결 강도·속도, 공격 틱, enemyReach, 웨이브 곡선 3값, 빈 라인 푸시 배율·슬랙
- **메타/방어**: 보스 XP/웨이브, 쿨 하한, 재생 전열 여유, 벽 HP·접촉피해·밀치기, 궁수 최대 수·사거리·ATK·탄약·간격
- **XP 테이블** Lv1→2 … 9→10
- **스킬 랭크당 수치** (`perRank` 등). 진격 `perRank`는 UI에서 숨김
- **적 스탯** HP / ATK / 라인 가속
- **아군 스탯** HP / ATK / 저지력 / 사거리

전장 칸 수·inset은 어드민 노트로만 안내(직접 칸 수 슬라이더는 없음 — 스킬 `boardWidth`).

---

## 7. 배포 상태

- 로컬: `npm run dev` → **http://localhost:5173**
- Git remote는 `https://github.com/Hyo84/MD.git` (branch `master`)까지는 있음
- **GitHub CLI 로그인 대기**. Vercel 배포는 그 다음(`.vercel`은 gitignore)
- 공개 URL·프로덕션 빌드 확인은 아직 없음

---

## 8. 제미나이에게 부탁할 검증 포인트

코드에 없는 이상적인 수치는 제안해도 되지만, **현재 테이블을 기준으로** 재미·난이도·성장이 맞는지 봐 달라.

1. **근접 사거리**: T1 range 36 vs 고블린 공격 도달 `15+24=39`. 저티어가 맞고 때리지 못하는 구간이 있는지, 열 전투로 의도된 건지.
2. **사거리 축소 후 T5–T10**: 클레브 56, 성기사 힐 110, 영웅 168이 3칸(250px) / 7칸(450px)에서 과잉·과소인지.
3. **발사 버스트 720 px/s × 0.12–0.28s 후 행군 28 px/s** vs 적 합류 **80 px/s**. 진격 스킬이 체감되는지, 첫 접촉이 너무 늦은지.
4. **빈 라인 전진 없음 + 아군만 푸시 업**: 웨이브 사이 숨 고르기가 너무 관대한지, 합류 중인 적만 있을 때 「정지」가 혼란스러운지.
5. **뒷열 비푸시 + 앞열 사망 compact**: 트롤/보스가 앞을 막은 채 뒷열이 무임승차하지 않는지, compact가 갑자기 라인을 다시 가속하는지.
6. **보스 speed 38** vs 기본 라인 6 + 고블린 3. W1 보스(HP 2300)가 전열 미구성 런을 한 방에 뚫는지.
7. **웨이브 곡선**: 1–10은 +10%/웨이브, 11부터 ×1.3. 스킬 해금(Lv5 5칸, Lv8 궁수)과 곡선 변곡이 맞물리는지.
8. **영웅 사명 80,000**: T10 ATK 6500 / 0.8s + 아서 광역. 승천이 너무 늦거나 빨라 전열 공백(T5 교체)이 밸런스를 깨는지. HP 사망 시 보상 없음이 리스크인지.
9. **발사 쿨 1.15 → 스킬**: 머지 엔진(보드에 유닛을 쌓아야 함)과 전열 DPS의 우선순위. 쿨감 step 2(홀수 레벨)가 성장감을 해치는지.
10. **전장 3칸 기본**: 머지 공간 vs 사격 통로. 5/7칸 해금이 Lv5/7이라 초반이 답답한지, 의도된 압박인지.
11. **XP/스킬 성장**: 보스 XP `200×wave` + 처치 점수가 Lv2를 1웨이브에, Lv5를 웨이브 5–7에 두는지. 포인트가 벽/궁수/전장에 묶여 전투 트리가 빈약한지.
12. **어드민 liveMult 0.5–5**: 곡선과 곱해 W11+에서 테스트 가능한지, HP 스냅샷 vs ATK 즉시 반영이 체감 왜곡을 만드는지.

검토 후: (a) 유지해도 되는 것, (b) 숫자만 손보면 되는 것, (c) 규칙 자체를 바꿔야 하는 것을 구분해 주면 된다.
