# KnightSlide — Gemini 검증용 정리 (2026-08-27)

**최신본: `GEMINI_VERIFY_2026-08-31.md`.** 이 파일은 8/27 스냅샷이다. 마지노 684, 캠프 32/162, 궁수 레벨, 티어 잠금, 허들 곡선이 여기와 다르다.

플레이: **https://md-knightslide.vercel.app**  
로컬: `npm run dev` → `http://localhost:5173/`  
저장소: https://github.com/Hyo84/MD (`master`)  
소스 기준(이 문서 작성 시점): `src/config.js`, `src/game.js`, `src/meta.js`, `src/renderer.js`, `src/hud.js`, `src/admin.js`, `src/assets.js`, `src/viewScale.js`, `src/skills-ui.js`, `src/style.css`, `index.html`

**검증은 이 파일 + 현재 소스를 우선한다.** `SPEC.md`, `GEMINI_BRIEF.md`, `GEMINI_VERIFY.md`(2026-08-26)는 라인 상한 90, 스폰 공식, 사거리, 처치 수, 상점 가격, 몬스터 스탯이 코드와 어긋난 곳이 있다.

좌표: 캔버스 **450×800**, 원점 좌상단, Y 증가 = 아래. UI/카피 한국어.

---

## 0. 한 줄 소개

중세 슬라이드·머지 디펜스. 하단에서 **가로 위치만 골라 수직 발사**하고, 같은 티어끼리 합쳐 강해진다. 적은 개별 행군이 아니라 **하나의 웨이브라인**에 붙고, 라인 전체가 마지노선(Y=660)으로 내려온다. 패배는 유닛 전멸이 아니라 **라인이 마지노선에 닿는 것**.

기술: Vite 8 + Vanilla JS + Canvas 2D + Matter.js 0.20. 아군만 물리 바디. 적·라인·궁수·벽은 비물리.

---

## 1. 2026-08-27 검증 대상 (이번 세션)

### 열 누수 (웨이브라인 진격)

이전: 전장 전체 **한 줄 줄다리기**. 한 열에 아군이 많이 쌓이면 그 저지력 합이 라인 전체에 적용되어, **막지 않은 열에 적이 착지해도** 라인이 멈추거나 밀려 올라갔다. 합류 중인 적만 있어도 막힌 열의 빈 라인 푸시가 전선을 뒤로 끌었다.

현재 (`_updateLine`):

1. **착지 전열(`_enemyOccupiesLine`)이 하나라도 있으면**
   - 사거리에 안 닿는 전열 적(`unheld`)이 있으면  
     `netSpeed = baseLineSpeed + unheld 적 speed 합 × liveMult`  
     → 다른 열 저지력은 **무시**. 막히지 않은 열이 라인을 내린다.
   - 모든 전열 적이 누군가의 사거리 안이면  
     예전 줄다리기 `base + 전열 speed 합 − 교전 아군 저지력 합`. 이기면 라인 상승.
2. **착지 전열이 없으면**
   - 살아 있는 적 중 **착지 슬롯을 막을 전열 아군이 없는 열**이 있으면 `netSpeed = 0` (빈 라인 후퇴 없음, 착지 대기).
   - 그런 적이 없으면 전열 아군 저지력으로 `LINE_START_Y`(228)까지 밀어올림.
3. 착지 보스가 occupy 중이면 위 결과와 무관하게 `netSpeed = max(netSpeed, bossMinAdvance=18)`.
4. 사거리가 옆 열까지 닿는 유닛(중앙 T10 등)은 그 열을 **막은 것**으로 본다. 의도.

occupy 조건: 살아 있음, `joining` 아님, 전열(row 0 또는 보스), `lineY−2 ≤ y ≤ lineY+8`.

합류: 캠프 Y=64에서 슬롯까지 `enemyJoinSpeed` 80px/s (라인이 더 빠르면 따라잡기 보너스). 합류 중 적은 라인 가속에 안 넣음.

### 같이 보면 되는 최근 기능 (이미 공유 배포됨)

- **자동발사:** 누르고 있으면 연속 발사. 하단 **자동** 토글 / Space. `localStorage md.knightslide.autofire.v1`. 재시작 후에도 유지. 홀드는 재시작 시 해제.
- **하단 HUD:** 상점 바로 위 **자동 / 다음 / 골드** 같은 높이.
- **방어벽 HP 게이지:** 마지노선 위(`DEFEAT_Y − 20`). 기본 목책 `bg_basicwall.png`, 스킬 설치 후 `bg_wall.png`.
- **리스폰 배율:** HUD·어드민 0.2~5.0 ×0.1. 캠프 러시 플로어 **이후**에 곱함.
- **로스터 리튠:** 아래 몬스터/아군 표. 해골기사는 보스 호위 전용(`bossOnly`).

---

## 2. 좌표·전장

| 상수 | 값 | 의미 |
|------|----|------|
| `CANVAS_W/H` | 450×800 | 논리 캔버스. `#wrap`도 9:16 fit |
| `CAMP_DEST_Y` | 64 | 적 캠프 상단·스폰 Y |
| `CAMP_DRAW_H` | 164 | 캠프 이미지 높이 |
| `LINE_START_Y` | **228** | 웨이브라인 상한 = 캠프 하단. 90 아님 |
| `ENEMY_SPAWN_Y` | 64 | 합류 시작 |
| `DEFEAT_Y` | 660 | 마지노선. 접촉 시 패배(벽 있으면 충격) |
| `LAUNCHER_Y` | 700 | 발사 Y |
| `BASE_SLOT_COLS` | 3 | 전장 확장 0랭크 |
| `MAX_SLOT_COLS` | 7 | 2랭크 |
| 슬롯 간격 | 3칸 ≈85px, 5칸 ≈67.5px, 7칸 ≈61.7px | `getSlotX` |
| 플레이어블 X | 3칸 100–350, 5칸 50–400, 7칸 0–450 | extra inset |
| `SLOT_ROW_H` | 46 | 적 스택 행 간격 |

전장 확장: 랭크 0/1/2 → **3 / 5 / 7**칸 (`slotColsForRank`). 해금 레벨 5, 랭크당 레벨 +2.

아군 라인 불변식: `y ≥ lineY + 14 + r`. 홀드 위치: `lineY + 20 + r`. 빈 라인 전열 슬랙 32px. 재생 전열 슬랙 48px.

Matter: 중력 0. 발사체는 `CAT_SHOT`(정착 아군과 비충돌). 정착 후 `CAT_ALLY`. `setVelocity`는 16.67ms당 px → `vel = pxPerSec / 60`. 발사 버스트 0.12~0.28초 후 정착, 이후 `advanceSpeedByRank`로 걸음.

---

## 3. 코어 루프

### 발사
- 하단 드래그로 X만. 수직 등속 `launchSpeed` 720px/s.
- 기본 쿨 **1.35s**. 스킬 쿨감 랭크당 −0.32s, 하한 **0.28s**. R3≈0.39s, R4+는 하한.
- 기본 롤: T1 75% / T2 25% (`LAUNCH_T2_BASE`). 정예신병으로 T3~T5 해금.
- T2–T9는 골드 상점 장전(필드 투하 아님). T1 구매 불가, T10은 T9+T9만.
- 누르고 있거나 자동발사 ON이면 쿨마다 `_launchUnit`.
- 돌격 저지력: 미정착 유닛이 occupy 적과 첫 접촉 시 `chargeStutterT=0.2s` 동안 `netSpeed=0`.

### 머지
- 같은 티어 충돌. **만피 불필요.** T10끼리는 머지 없음.
- T9+T9 → 아서/잔느/발키리 랜덤. 사명 게이지를 채우면 승천.
- 머지 점수 `newTier * 5`. 발사체 연속 2회+ 머지 시 남은 쿨 × (1 − `mergeComboCdRefund` 0.5).
- 머지 충격 스킬 있으면 범위 피해·넉백·아군 힐. 팽창 넉백 `MERGE_BLAST_RADIUS` 80, `FORCE` 360.

### 패배
- `lineY ≥ 660`. 벽 스킬 있으면 HP 소모 + `wallKnockback`만큼 위로(상한 228). 그 충격으로 HP 0이면 벽 파괴·궁수 삭제, **밀치기는 적용**. 다음 접촉이 게임오버.
- 기본 벽 HP 3, 타격당 −1, 밀치기 70px.

---

## 4. 웨이브라인 공식 (현재)

```
joined = occupy하는 전열 적
unheld = joined 중 사거리 안 아군이 없는 적

if chargeStutterT > 0:
  net = 0
elif joined.length == 0:
  if 막지 않은 열로 적이 오고 있으면: net = 0
  else: net = −(전열 아군 stop 합 × emptyLinePushScale)
else if unheld.length > 0:
  net = baseLineSpeed + Σ unheld.speed * liveMult   # 기절 적은 제외
else:
  net = baseLineSpeed + Σ joined.speed * liveMult − Σ 교전 아군 stop
if 착지 보스 occupy: net = max(net, bossMinAdvance)
lineY += net * dt
lineY = clamp(LINE_START_Y, …); ≥ DEFEAT_Y → 벽/게임오버
```

- `baseLineSpeed` **8**. `bossMinAdvance` **18**.
- 라인 가속에 **웨이브 곡선 미적용**. HP/ATK만 `waveMultiplier × liveMult`. speed에는 `liveMult`만.
- 뒷열(row≥1)은 occupy 아님. 앞열 죽으면 compact 후 뒷열이 행군해 메움. 붙기 전에는 밀지 않음.

---

## 5. 아군 스탯 (`UNITS`)

stop = 교전 저지력(및 빈 라인 푸시) px/s. range = 표면간 거리 사거리 px (`hypot − r1 − r2`).

| T | 이름 | r | drawR | HP | ATK | mass | stop | range | 특수 |
|---|------|---|-------|----|-----|------|------|-------|------|
| 1 | 민병대 | 16 | 14 | 50 | 5 | 1.0 | 4 | 18 | |
| 2 | 신병 | 18 | 16 | 110 | 12 | 1.3 | 6 | 21 | |
| 3 | 경보병 | 20 | 18 | 240 | 25 | 1.7 | 9 | 25 | |
| 4 | 중보병 | 22 | 20 | 500 | 55 | 2.2 | 13 | 29 | |
| 5 | 기사단원 | 24 | 22 | 1050 | 120 | 2.8 | 18 | 35 | 클레브 r28 ×0.5 |
| 6 | 근위대장 | 26 | 27 | 2200 | 250 | 3.5 | 24 | 41 | 기절 10% / 0.5s |
| 7 | 성기사 | 28 | 28 | 4600 | 520 | 4.3 | 32 | 48 | 2초마다 r110, maxHp 4% |
| 8 | 드래곤가디언 | 30 | 29 | 9500 | 1100 | 5.3 | 42 | 56 | 화상 atk 20% / 3s |
| 9 | 대원수 | 33 | 30 | 19000 | 2300 | 6.5 | 55 | 64 | 4초마다 사거리 내 atk 30% |
| 10 | 영웅 | 38 | 33 | 50000 | 6500 | 10.0 | 90 | 84 | 직업 스킬 |

공격 틱 **0.8s**. 잔느 오라 r140, 기본공격·T5 클레브만 ×1.5 (영웅 자신·T7/T9/궁수 미적용).

진군 px/s (`advanceSpeedByRank`): R0 28 / R1 34 / R2 42 / R3 52 / R4 64 / R5 80. 발사 관성 아님.

### 영웅
- 아서: 3초마다 전 적 atk×0.4
- 잔느: 오라 공격력 +50%
- 발키리: 0.3초마다 사거리 내 atk×0.12
- 사명 피해: `max(20000, 15000 + wave×5000)`. 스폰 시 고정. W1=20000, W13=80000.
- 승천: atk×1.5 전 적, 라인 228로 리셋, 점수 +400, 자리에 T5. 슬로모 0.3s.
- HP 사망: 승천 없음. 생존 중 보스 3킬이면 약한 퇴장(T5만).

---

## 6. 적 (`MONSTERS`)

speed = occupy 시 라인 가속 기여 px/s.

| key | 이름 | 등급 | r | HP | ATK | speed | 점수 | 골드 | 비고 |
|-----|------|------|---|----|-----|-------|------|------|------|
| goblin | 고블린 | 1 | 15 | 48 | 5 | 4 | 10 | 6 | |
| skeleton | 스켈레톤 | 2 | 16 | 150 | 12 | 1 | 20 | 12 | 스프라이트 `enemy_skeleton2.png` |
| orc | 오크 | 3 | 22 | 185 | 14 | 2 | 30 | 18 | W2+ |
| troll | 동굴 트롤 | 4 | 28 | 430 | 15 | 3 | 100 | 50 | W3+ ~4%. 피격 1.2s 후 maxHp 8%/s |
| skelknight | 해골기사 | 5 | 24 | 280 | 21 | 3 | 80 | 40 | `bossOnly`, 스프라이트 `enemy_skeleton.png` |
| boss | 오크 워로드 | 보스 | 45 | 1350 | 17 | 12 | 500 | 150 | 슬롯 미점유, 중앙 |

실효 HP = round(기본 HP × waveMult × liveMult) 스냅샷. ATK·라인 speed는 liveMult 즉시.

적 근접 사거리 = `적.r + enemyReach(12)`.

### 스폰 풀 (`_monsterPool`)
- W1: 고블린 78 / 스켈 12
- W2+: 고블린 55 / 스켈 25 / 오크 20
- W3+: 트롤 가중 4 추가
- 해골기사는 풀에 없음. 보스 등장 시 기본 2마리.

### 웨이브 할당량
`killsNeeded(wave) = 12 + wave×2` → W1=14, W2=16, W11=34.  
잡몹 스폰은 `waveTrashSpawned ≥ need` 또는 `kills + 살아있는 잡몹 ≥ need`면 중단. 보스전 스폰은 할당량에 안 넣음.

할당량 달성 → 경고 **1.6s** → 보스 + 해골기사 2 + 나머지 호위(총 호위 4, 필드 잡몹 캡 5). 보스 처치 시 잔당 도주(점수 없음), 웨이브+1, kills 0, 상점 한도 리셋, 궁수 탄약 리필. 보스 XP 보너스 `200 × 클리어한 웨이브`.

보스 집결: **등장 시점에 있던 아군만** `gatherToBoss`. 이후 발사는 진격 경로 유지. `bossGatherStrength` 0.6 < 0.7이라 **교전 중 유닛은 집결 안 함**.

---

## 7. 난이도 곡선

```
gentle: mult = 1 + (min(wave, 10) − 1) × 0.20
W11+: × 3.0 × 1.45^(wave−11)
```

| W | waveMult |
|---|----------|
| 1 | 1.00 |
| 5 | 1.80 |
| 10 | 2.80 |
| 11 | 8.40 |
| 12 | 12.18 |

목표 주석: 스킬 0 기준 **W11 마지노에서 게임오버**.

실시간 `liveMult` 0.5~5.0 step 0.1. 재시작 유지. 기존 적 HP 비율 유지 리스케일, ATK/라인 즉시.

### 스폰 간격
`interval = max(0.55, 2.3 / (1 + (wave−1)×0.30))` × 지터 0.82~1.18.

대략: W1 2.3s, W5 ≈1.05s, W10 ≈0.62s, 이후 하한 0.55s.  
첫 스폰 타이머 = `spawnInterval × 0.75` ≈ 1.73s.

캠프 접근: 전열이 캠프 하단에서 200px 안이면 타이머 가속, 닿으면 ×6 (ease 1.5). W1 실효 ≈0.38s. 러시 플로어 **0.28s**.  
캠프 스프라이트 안 침입: ×2~×3 추가.  
그 다음 `spawnRateMult`(기본 1)를 곱함.

---

## 8. 경제·상점

런 시작 골드 = `ECONOMY.startGold(0) + 초기자금 랭크×50`.  
세금 = 랭크당 1.5G/s. 바운티 = 처치골드 × (1 + 랭크×0.10).

| T | 가격 | 웨이브 한도 |
|---|------|-------------|
| 2 | 80 | 2 |
| 3 | 200 | 2 |
| 4 | 380 | 2 |
| 5 | 600 | 2 |
| 6 | 1400 | 2 |
| 7 | 3000 | 1 |
| 8 | 6500 | 1 |
| 9 | 14000 | 1 |

기본 구매 최대 티어 **T2**. 용병술 랭크당 +1 (`shopMaxTier = min(9, 2 + rank)`). 최대 랭크 6 → 공식상 **T8**. T9는 치트 없이는 안 열림(검증 포인트).

치트 On: T2–T9 무료·무한·해금 무시. T1/T10은 구매 불가. `md.knightslide.cheats.v1`.

---

## 9. 메타 (레벨·스킬)

`localStorage md.knightslide.meta.v1`. 런 스코어는 저장 안 함. XP = 점수 + 보스 보너스.

Lv1→2 800 XP, 이후 테이블 1300…12000. Lv10+ : 12000 + 3000×(level−9). 레벨업 시 포인트 +1. 스킬 초기화는 랭크만 환불, 포인트 = level−1.

| id | 이름 | 트리 | max | 해금Lv | 효과 |
|----|------|------|-----|--------|------|
| launchCd | 발사 쿨감 | 전투 | 5 | 1 (step 2) | −0.32s/랭크, 하한 0.28 |
| advance | 진격 속도 | 전투 | 5 | 1 | 테이블 28→80 |
| regen | 재생 | 전투 | 5 | 1 | 전열/교전 maxHp 0.4%/s/랭크 |
| stopping | 저지력 증가 | 전열 | 5 | 1 | +8%/랭크 |
| boardWidth | 전장 확장 | 전열 | 2 | 5 | 3→5→7칸 |
| eliteRecruit | 정예신병 | 머지 | 10 | 1 | T2~T5 확률. R10 T5=5% T1=50% |
| mergeShock | 머지 충격 | 머지 | 5 | 3 | 피해 18/랭크, r 30+6, KB 6, 힐 4% |
| wall | 방어벽 설치 | 벽 | 1 | 3 | |
| wallHp | 방어벽 강화 | 벽 | 5 | 5 | +2 HP/랭크 |
| wallKb | 밀치기 거리 | 벽 | 5 | 5 step2 | +22px/랭크 |
| archer | 궁수 해금 | 궁수 | 1 | 8 | 벽 선행 |
| archerCount | 궁수 수 | 궁수 | 4* | 8 | 실효 max `archerMax−1`=2 → 최대 3명 |
| archerRange | 궁수 사거리 | 궁수 | 5 | 8 | 80+18/랭크 |
| archerAtk | 궁수 공격력 | 궁수 | 5 | 8 step2 | 7+4/랭크 |
| archerAmmo | 궁수 탄약 | 궁수 | 5 | 8 | 10+4/랭크, 1.05s/발 |
| startGold | 초기 자금 | 경제 | 10 | 1 | +50G |
| taxRate | 세금 징수 | 경제 | 10 | 2 | +1.5G/s |
| bountyGold | 바운티 | 경제 | 10 | 3 | +10% |
| mercenary | 용병술 | 경제 | 6 | 4 | 구매 최대 티어 |

궁수 탄약은 런 시작·보스 처치(다음 웨이브) 때만 리필. 소진 시 침묵.

---

## 10. HUD·조작·표시

- 상단: 웨이브, 처치 n/need, 점수, 웨이브배율, liveMult −/+, 범위 토글(끄기/아군만/전체).
- 하단 독: **자동 / 다음(미리보기) / 골드**. 그 아래 T1–T10 상점/치트 바. 상점 T1 라벨은 「발사」(자동 버튼과 별개).
- 벽 HP: 마지노선 **위**.
- 왼쪽 숲 세로: 배율·스킬. 오른쪽: 재시작·⚙. 보드 높이를 깎지 않음.
- 키보드: `+`/`-` 난이도, Space 자동발사, A 어드민.
- 모바일: `#wrap` `min()` fit. `setupViewScale`가 에셋 로드 **전에** 돌아 로딩 화면이 왼쪽으로 안 붙음. `justify-content: center`.

스프라이트: `public/sprites/` (루트 `/sprites/`는 gitignore). 아군 `ally_t01`~`t10` 뒷모습, 적 앞모습. 스켈레톤=`skeleton2`, 해골기사=`skeleton`. 배경 field/forest/camp. 벽 basic/built.

---

## 11. 물리·합류 디테일 (회귀)

- 발사 오버슈트 방지: Matter 0.20 속도 단위, 발사체 카테고리, 캠프 근접은 **정착** 아군만.
- 적 슬롯은 반지름 기반. 보스가 가리는 칸은 뒷열. 겹치면 합류 재배치.
- 합류 중 `destY`가 위로 가도 y를 올리지 않고 joining 유지(조기 착지 버그 방지).
- `dt` 캡 0.05s, 엔진 스텝 캡 33.33ms.

---

## 12. 플레이 검증 체크리스트

### 열 누수 (필수)
1. 3칸 전장, **왼쪽 열에만** T1을 여러 명 두고 가운데·오른쪽에 고블린이 라인에 착지하면 **라인이 내려가야** 한다. 예전처럼 왼쪽 저지력 때문에 멈추거나 올라가면 실패.
2. 왼쪽만 막은 채 오른쪽 열로 적이 **합류 중**이면 라인이 빈 라인 푸시로 올라가지 않고 기다린다. 착지 후 1과 같이 내려간다.
3. **중앙 T10**처럼 사거리가 3열을 덮으면 전열을 막은 것으로 보고, 줄다리기에서 라인을 올릴 수 있다. (누수 아님)
4. 착지 보스 + 강한 저지력: 줄다리기가 음수여도 **최소 18px/s**로 내려간다. 합류 중인 보스에는 플로어 없음.

### 루프
5. W1 스폰 약 2초 전후. 필드 잡몹이 `처치 0/14`인데 14를 크게 넘으면 실패.
6. 14킬 → 경고 → 보스+해골기사 2+일반 호위가 캠프에서 내려옴. 보스전 중 잡몹 캡 5.
7. 보스 처치 → 잔당 도주, 웨이브 2, 할당량 16, 상점 한도 리셋.
8. 빈 라인(적 없음)에서 전열 아군이 있으면 라인이 **228까지** 올라가고 그 위로는 안 감.
9. 라인 상한에서 적도 64→228로 걸어와 착지한 뒤에야 전진.
10. 자동: 토글 ON이면 손 떼도 발사. 누르고 있으면 토글 없이도 발사. Space 토글. 재시작 후 토글 유지, 홀드 해제.
11. 상점: T1 클릭은 장전 안 됨. T2는 80G·웨이브 2회. 용병술 0이면 T3+ 자물쇠.
12. 벽 게이지가 마지노선 **위**. 스킬 없으면 목책 스프라이트, 있으면 석벽.
13. 모바일 로딩이 화면 왼쪽에 붙지 않고 중앙.
14. 치트 기본 Off.

---

## 13. 의도적으로 애매한 점 (버그로 단정하지 말 것)

- 전역 `lineY`는 유지. 열마다 라인이 따로 내려가지 않음. 막힌 열은 기여 0, 안 막힌 열만 전체 라인을 민다.
- T7+ 사거리·좁은 7칸에서는 한 유닛이 옆 열까지 막을 수 있음. 사거리 설계.
- 합류 중 적은 라인을 안 민다. 착지 전 “빈 열이 있는데 라인이 안 내려감”은 수 초간 정상.
- 보스전 부하를 잡아도 웨이브 처치 수는 거의 의미 없음(보스 처치 시 kills 리셋). 점수는 줌.
- 라인 speed에 웨이브 곡선 없음. 후반 압박은 개체 수·보스 플로어·liveMult·W11 HP 클리프.
- 트롤 재생은 maxHp 비율. 때리는 중에는 꺼짐.
- 궁수 솔로 웨이브는 의도적으로 어렵게(탄약·딜).
- 7칸이면 숲 HUD가 플레이 가장자리와 겹칠 수 있음.
- 용병술 만랭이 T8까지인 것은 코드 그대로. T9 상점 해금 공백은 밸런스 질문.
- Node 테스트는 assertion 통과 후 `assets.js`가 `document is not defined`로 exit 1 날 수 있음. 테스트 실패로 보지 말 것.

---

## 14. 원 기획 대비 편차

1. README의 슬링샷 조준은 구버전. 지금은 수직 발사.
2. 머지 만피 요구 없음.
3. 패배는 마지노 접촉. 벽이 있으면 1회 충격.
4. 스켈레톤 speed=1 (0 아님). 보스 speed=12 (38 아님).
5. 영웅은 15초 타이머가 아니라 사명 피해 쿼터.
6. 브랜딩 부제 `[프로토타입 0.0.260826]`.

---

*문서 생성: 2026-08-27. 워크스페이스 소스 현재 구현. 어드민으로 바꾼 런타임 값은 이 기본값과 다를 수 있다. 제미나이에는 이 파일 본문을 그대로 붙여 넣으면 된다.*
