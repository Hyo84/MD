# KnightSlide — Gemini 검증용 정리 (2026-08-26)

**최신본: `GEMINI_VERIFY_2026-08-27.md`.** 이 파일은 8/26 스냅샷이며 상점 가격·몬스터 스탯·열 누수 로직이 코드와 다를 수 있다.

플레이: **https://md-knightslide.vercel.app**  
로컬: `npm run dev` → `http://localhost:5173/`  
소스 기준: `src/config.js`, `src/game.js`, `src/renderer.js`, `src/hud.js`, `src/meta.js`, `src/skills-ui.js`, `src/admin.js`, `src/assets.js`, `src/viewScale.js`, `src/style.css`, `index.html`

**주의:** `SPEC.md`, `GEMINI_BRIEF.md`는 이전 수치(라인 상한 90, 스폰 공식, 사거리, 처치 수, 발사 쿨 1.9, 사명 80000 등)가 코드와 어긋난다. 검증은 **이 파일 + 현재 소스**를 우선한다.

좌표: 캔버스 450×800, 원점 좌상단, Y 증가 = 아래.

---

## 2026-08-26 저녁 — 골드 경제 / 상점 / 템포 (이번 검증 대상)

무료 직선 발사는 그대로다. T1은 계속 무료 롤. 하단 티어표는 **상점**이다.

### 템포
- 기본 발사 쿨 **1.35s** (스킬 쿨감, 하한 0.28s). 어드민 `전투 / 템포` → 발사 쿨다운.
- 발사체(미착지)로 **연속 2회 이상** 머지 시 `"Combo xN!"` 및 남은 발사 쿨 × (1 − 0.5). 착지하면 콤보 리셋. 어드민 `머지 콤보 쿨감 비율`.
- 영웅 사명: `max(20000, 15000 + wave * 5000)`. W1=20000, W13=80000. 스폰 시점에 고정. 어드민 최소/기본/웨이브당.

### 경제 스킬 (트리: 경제)
- `startGold` 초기 자금: 랭크당 +50G, 최대 10. **런 시작 시** 지급.
- `taxRate` 세금 징수: 랭크당 초당 +1.5G, 최대 10. 즉시 적용.
- `bountyGold` 바운티 헌터: 처치 골드 +10%/랭크. 전역 배율 `ECONOMY.bountyMult`와 곱함.
- `mercenary` 용병술: 기본 구매 최대 **T3**. 랭크당 T4→…→T9 (최대 6랭크).

처치 골드(점수와 별도): 고블린 10 / 오크 25 / 스켈 20 / 트롤 80 / 보스 300.

### 하단 상점
- T1 구매 불가(무료 발사). T10 구매 불가(T9+T9).
- T2 50G ∞ · T3 120G 5/웨이브 · T4 280G 3 · T5 600G 2 · T6 1400G 2 · T7 3000G 1 · T8 6500G 1 · T9 14000G 1.
- 클릭 시 필드 투하가 아니라 **발사대 `currentTier`에 장전**. 그다음 조준 발사.
- 보스 처치 → 다음 웨이브 진입 시 T3–T9 구매 횟수 리셋 (T2는 ∞).
- 아이콘 위: 가격 + 한도(`3/3`, `∞`). 잠긴 티어 자물쇠 + dim. 골드/한도 부족도 dim.
- 치트 On: T2–T9 무료·무한·해금 무시. T1/T10은 여전히 구매 불가.

### 어드민
- 치트 On/Off (기본 Off).
- **경제:** 보유 골드 ±1000 / 직접 입력, 초기 자금·세금·바운티 배율, 티어별 가격/한도(-1=∞), 몬스터 처치 골드.
- **전투/템포:** 발사 쿨, 콤보 쿨감, 영웅 사명 3값.

---

## 이전에 손본 것 (회귀)

### 1. 적 스폰 페이스

- 1웨이브는 느리고, 웨이브가 오를수록 빨라진다.
- 공식: `interval = max(0.9, 3.5 / (1 + (wave-1) * 0.22))` 초, 지터 `×(0.82~1.18)`.
- 대략: W1 ≈ 3.5s, W5 ≈ 1.86s, W10 ≈ 1.17s, 이후 하한 0.9s.
- 어드민: `spawnInterval`, `spawnIntervalAccel`, `spawnIntervalFloor`.

### 2. 웨이브 처치 할당량 (잡몹 상한)

- `killsNeeded(wave) = 8 + wave * 2` → W1=10, W2=12, W3=14 …
- 잡몹은 **이번 웨이브에서 나온 수**가 할당량을 넘으면 더 안 나온다. (`waveTrashSpawned` + 살아 있는 잡몹/`kills` 이중 가드)
- **버그 수정:** 처음에 살아있는 적을 `units`(아군)에서 세서 상한이 안 걸렸다. 지금은 `enemies`에서 센다.
- HUD `처치 n / need`가 차기 전에 필드 잡몹이 need를 크게 넘으면 회귀.

### 3. 캠프 침입 시 투입 가속

- 적 캠프 스프라이트: Y **64 ~ 228** (`CAMP_DEST_Y=64`, `CAMP_DRAW_H=164`).
- 정착 아군이 이 Y 구간에 있으면 스폰 타이머가 **2배**(1명) ~ **3배**(여럿).
- 아군은 웨이브라인을 넘지 못하므로, 라인이 캠프까지 밀렸을 때만 발동.

### 4. 보스전 부하 + 합류 연출

- 할당량 달성 → 경고 1.6초 → 보스와 호위 부하가 **캠프 상단(Y 64)**에서 내려와 라인에 착지. (예전: 보스가 라인에 즉시 등장, 보스전 중 잡몹 0)
- 호위 기본 **3**마리. 보스전 중 필드 잡몹 상한 **4**. 보스전 스폰은 웨이브 할당량에 안 넣음.
- 보스 처치 시 남은 잡몹은 점수/처치 없이 사라지고 `잔당 도주!`. 그다음 웨이브 +1, 처치 수 0.
- 라인이 이미 상한(캠프 하단)이면 이동 거리가 짧아 거의 바로 착지할 수 있음. 정상.

### 5. 웨이브라인 상한 = 캠프 하단

- `LINE_START_Y = 228` (캠프 64+164). 밀어올려도 캠프 이미지 밑을 넘지 않음.
- 예전 90까지 밀리면 적이 나오자마자 전열에 붙어 즉사하는 느낌이 났음. 중간(146)은 캠프와 겹쳐 보였음.
- `ENEMY_SPAWN_Y = 64` (캠프 위). 라인이 228이어도 내려오며 합류.

### 6. 모바일 / HUD 버튼

- 화면 크기(1배~전체)·재시작·⚙·스킬은 **캔버스 위 가로 바가 아님**. 보드 높이를 깎지 않음.
- 왼쪽 숲 세로: 1배 / 2배 / 3배 / 전체 / 스킬.
- 오른쪽 숲 세로: 재시작 / ⚙.
- 전장 7칸이면 숲이 거의 없어 가장자리에 붙음.

### 7. 치트 기본 Off

- 어드민 → 치트 Off/On. 기본 Off. `localStorage` `md.knightslide.cheats.v1`.
- On일 때만 하단 T1–T10 바로 다음 발사 티어 고정.
- Off에서 바를 눌러도 발사는 안 함(HUD 클릭 소비).

### 8. 아군 스프라이트 선명도

- 원인은 픽셀아트 nearest 축소. 지금은 고품질 보간 + T1~T5 `drawR` 11/13/15/17/19 → **14/16/18/20/22**.
- 물리 반경 `r`은 그대로. 조금 부드러워 보이는 것은 의도.

---

## 건드리지 않은 핵심 루프 (회귀만 보면 됨)

- 하단 X 조준 + 수직 발사. 기본 쿨 **1.35s**, 스킬로 감소, 하한 **0.28s**. T2–T9는 골드로 장전 가능.
- 같은 티어 충돌 머지. T9+T9 → 랜덤 T10 영웅. 영웅 이름은 스프라이트에 안 그림.
- 영웅 사명: `max(20000, 15000+wave*5000)` 승천 / 생존 중 보스 3킬 시 약한 퇴장(T5).
- 패배: 라인 Y ≥ **660**. 유닛 전멸 ≠ 패배. 벽 있으면 1회 충격.
- 보스 집결: **등장 시점에 있던 아군만** `gatherToBoss`. 이후 발사는 자기 레인.

적 기본값: 고블린 HP32 ATK3 speed2 / 오크 110/8/1 / 스켈 90/8/0 / 트롤 280/9/regen4 / 보스 900/12/speed8.

---

## 플레이 검증 체크리스트

1. **W1 스폰:** 처음 적이 천천히(약 2~4초 간격). 필드 잡몹이 `처치 0/10`인데 10마리를 크게 넘으면 실패.
2. **할당량 후 보스:** 10킬 → 경고 → 보스+부하가 캠프에서 내려옴. 보스전 중 부하는 최대 4.
3. **보스 처치:** 잔당 사라지고 웨이브 2. 다음 웨이브 할당량은 12.
4. **라인 상한:** 전열로 밀어올려도 라인이 캠프 **하단**(Y 228)에서 멈춤. 캠프 이미지는 라인 위.
5. **캠프 레이드:** 라인을 캠프 하단까지 민 뒤, 아군이 캠프 Y 구간(64–228)에 있으면 투입이 빨라짐. 아군은 라인을 못 넘으므로 보통 캠프 바로 아래에 선다.
6. **치트:** 기본 Off. T2–T9 유료 상점. 어드민 On 후 T2–T9 무료 장전.
7. **버튼:** 1배~전체/재시작/⚙이 화면 위를 가로막지 않고 숲 쪽 세로. 보드가 예전 상단 바만큼 작아 보이지 않음.
8. **아군 그림:** T1이 각진 모자이크처럼 깨지지 않음. (약간 부드러운 건 OK)
9. **모바일:** 스크롤/줌이 보드를 밀지 않음. 버튼 탭 가능.
10. **상점:** T1 클릭은 장전 안 됨. T2는 50G 있으면 장전되고 발사대 유닛이 T2로 바뀜. T4는 용병술 없으면 자물쇠.
11. **웨이브 한도:** T3를 5회 사면 더 이상 안 됨. 보스 잡고 다음 웨이브에서 다시 5.
12. **콤보:** 발사한 유닛이 연속 2번 머지되면 Combo x2 및 쿨이 체감으로 줄어듦.

---

## 의도적으로 애매한 점 (버그로 단정하지 말 것)

- 보스전 부하를 직접 잡아도 웨이브 처치 수에는 거의 의미 없음(보스 처치 시 kills 리셋). 점수는 줌.
- 7칸 전장에서는 숲 HUD가 플레이 가장자리와 겹칠 수 있음.
- `GEMINI_BRIEF.md`의 사거리·처치수·라인 90은 **구버전**.
